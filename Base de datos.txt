name: DATABASE
user: SYSTEM
pass: 123456


CREATE TABLE "MUNICIPIO" (
    "MUNICIPIO_ID"      NUMBER NOT NULL ENABLE,
    "NOMBRE"            VARCHAR2(400) NOT NULL ENABLE,
    "POBLACION_TOTAL"   NUMBER NOT NULL ENABLE,
    "POBLACION_A_VOTAR" NUMBER NOT NULL ENABLE,
    "VOTOS_TOTALES"     NUMBER NOT NULL ENABLE,
    CONSTRAINT  "MUNICIPIO_PK" PRIMARY KEY ("MUNICIPIO_ID") ENABLE
   );


CREATE TABLE "PARTIDO" (
    "PARTIDO_ID"   NUMBER NOT NULL ENABLE,
    "NOMBRE"       VARCHAR2(400) NOT NULL ENABLE,
    "SIGLAS"       VARCHAR2(400) NOT NULL ENABLE,
    "DIRECCION"    VARCHAR2(400) NOT NULL ENABLE,
    "MUNICIPIO_ID" NUMBER NOT NULL ENABLE,

    CONSTRAINT  "PARTIDO_PK" PRIMARY KEY  ("PARTIDO_ID") ENABLE,

    CONSTRAINT "PARTIDO_FK" FOREIGN KEY ("MUNICIPIO_ID") 
	REFERENCES "MUNICIPIO" ("MUNICIPIO_ID") ENABLE
   );



CREATE TABLE "CANDIDATO" (
    "CANDIDATO_ID"   NUMBER NOT NULL ENABLE,
    "NOMBRE"         VARCHAR2(400) NOT NULL ENABLE,
    "CEDULA"         VARCHAR2(400) NOT NULL ENABLE,
    "SEXO"           VARCHAR2(400) NOT NULL ENABLE,
    "EDAD"           NUMBER NOT NULL ENABLE,
    "TIPO"           VARCHAR2(400) NOT NULL ENABLE,
    "CANTIDAD_VOTOS" NUMBER NOT NULL ENABLE,
    "PARTIDO_ID"     NUMBER NOT NULL ENABLE,

    CONSTRAINT  "CANDIDATO_PK" PRIMARY KEY ("CANDIDATO_ID") ENABLE,
 
   CONSTRAINT "CANDIDATO_FK" FOREIGN KEY ("PARTIDO_ID")
	REFERENCES "PARTIDO" ("PARTIDO_ID") ENABLE

);




CREATE TABLE "COLEGIO" (
    "COLEGIO_ID"   NUMBER NOT NULL ENABLE,
    "NOMBRE"       VARCHAR2(400) NOT NULL ENABLE,
    "DIRECCION"    VARCHAR2(400) NOT NULL ENABLE,
    "MUNICIPIO_ID" NUMBER NOT NULL ENABLE,

    CONSTRAINT  "COLEGIO_PK" PRIMARY KEY ("COLEGIO_ID") ENABLE,

    CONSTRAINT "COLEGIO_FK" FOREIGN KEY ("MUNICIPIO_ID")
	REFERENCES "MUNICIPIO" ("MUNICIPIO_ID") ENABLE
);






CREATE TABLE "MESA" (
    "MESA_ID"         NUMBER NOT NULL ENABLE,
    "NUMERO"          NUMBER NOT NULL ENABLE,
    "VOTOS_EN_BLANCO" NUMBER NOT NULL ENABLE,
    "VOTOS_NULOS"     NUMBER NOT NULL ENABLE,
    "VOTOS_VALIDOS"   NUMBER NOT NULL ENABLE,
    "TOTAL_VOTOS"     NUMBER NOT NULL ENABLE,
    "ID_COLEGIO"      NUMBER NOT NULL ENABLE,

    CONSTRAINT  "MESA_PK" PRIMARY KEY ("MESA_ID") ENABLE,

    CONSTRAINT "MESA_FK" FOREIGN KEY ("ID_COLEGIO")
	REFERENCES "COLEGIO" ("COLEGIO_ID") ENABLE
);



CREATE TABLE "VOTANTE" (

    "NOMBRE"     VARCHAR2(400) NOT NULL ENABLE,
    "CEDULA"     NUMBER NOT NULL ENABLE,
    "SEXO"       NUMBER NOT NULL ENABLE,
    "EDAD"       NUMBER NOT NULL ENABLE,
    "DIRECCION"  VARCHAR2(400) NOT NULL ENABLE,
    "MUNICIPIO_ID" NUMBER NOT NULL ENABLE, 
    "MESA_ID"    NUMBER NOT NULL ENABLE,

    CONSTRAINT "VOTANTE_PK" PRIMARY KEY ("CEDULA") ENABLE,

   CONSTRAINT "VOTANTE_FK" FOREIGN KEY ("MESA_ID")
	REFERENCES "MESA" ("MESA_ID") ENABLE,

    CONSTRAINT "VOTANTE_FK2" FOREIGN KEY ("MUNICIPIO_ID")
	REFERENCES "MUNICIPIO" ("MUNICIPIO_ID") ENABLE
);


 

CREATE TABLE "JURADO"(

    "NOMBRE"     VARCHAR2 (400) NOT NULL ENABLE,
    "CEDULA"     NUMBER NOT NULL ENABLE,
    "EDAD"       NUMBER NOT NULL ENABLE,
    "MESA_ID"    NUMBER NOT NULL ENABLE,
    "COLEGIO_ID" NUMBER NOT NULL ENABLE,

    CONSTRAINT "JURADO_PK" PRIMARY KEY ("CEDULA") ENABLE,

    CONSTRAINT "JURADO_FK" FOREIGN KEY ("MESA_ID")
	REFERENCES "MESA" ("MESA_ID") ENABLE,

    CONSTRAINT "JURADO_FK2" FOREIGN KEY ("COLEGIO_ID")
	REFERENCES "COLEGIO" ("COLEGIO_ID") ENABLE
);



