name: DATABASE
user: SYSTEM
pass: 123456


CREATE table "MUNICIPIO" (
    "MUNICIPIO_ID"      NUMBER NOT NULL,
    "NOMBRE"            VARCHAR2(20),
    "POBLACION_TOTAL"   NUMBER,
    "POBLACION_A_VOTAR" NUMBER,
    "VOTOS_TOTALES"     NUMBER,
    constraint  "MUNICIPIO_PK" primary key ("MUNICIPIO_ID")
)
/

CREATE table "PARTIDO" (
    "PARTIDO_ID"   NUMBER NOT NULL,
    "NOMBRE"       VARCHAR2(20),
    "SIGLAS"       VARCHAR2(20),
    "DIRECCION"    VARCHAR2(20),
    "MUNICIPIO_ID" NUMBER NOT NULL,
    constraint  "PARTIDO_PK" primary key ("PARTIDO_ID")
)
/

ALTER TABLE "PARTIDO" ADD CONSTRAINT "PARTIDO_FK" 
FOREIGN KEY ("MUNICIPIO_ID")
REFERENCES "MUNICIPIO" ("MUNICIPIO_ID")

/


CREATE table "CANDIDATO" (
    "CANDIDATO_ID"   NUMBER NOT NULL,
    "NOMBRE"         VARCHAR2(30),
    "CEDULA"         VARCHAR2(20),
    "SEXO"           VARCHAR2(20),
    "EDAD"           NUMBER,
    "TIPO"           VARCHAR2(30),
    "CANTIDAD_VOTOS" NUMBER,
    "PARTIDO_ID"     NUMBER NOT NULL,
    constraint  "CANDIDATO_PK" primary key ("CANDIDATO_ID")
)
/

ALTER TABLE "CANDIDATO" ADD CONSTRAINT "CANDIDATO_FK" 
FOREIGN KEY ("PARTIDO_ID")
REFERENCES "PARTIDO" ("PARTIDO_ID")

/


CREATE table "COLEGIO" (
    "COLEGIO_ID"   NUMBER NOT NULL,
    "NOMBRE"       VARCHAR2(30),
    "DIRECCION"    VARCHAR2(30),
    "MUNICIPIO_ID" NUMBER NOT NULL,
    constraint  "COLEGIO_PK" primary key ("COLEGIO_ID")
)
/

ALTER TABLE "COLEGIO" ADD CONSTRAINT "COLEGIO_FK" 
FOREIGN KEY ("MUNICIPIO_ID")
REFERENCES "MUNICIPIO" ("MUNICIPIO_ID")

/

CREATE table "MESA" (
    "MESA_ID"         NUMBER NOT NULL,
    "NUMERO"          NUMBER,
    "VOTOS_EN_BLANCO" NUMBER,
    "VOTOS_NULOS"     NUMBER,
    "VOTOS_VALIDOS"   NUMBER,
    "TOTAL_VOTOS"     NUMBER,
    "ID_COLEGIO"      NUMBER NOT NULL,
    constraint  "MESA_PK" primary key ("MESA_ID")
)
/

ALTER TABLE "MESA" ADD CONSTRAINT "MESA_FK" 
FOREIGN KEY ("ID_COLEGIO")
REFERENCES "COLEGIO" ("COLEGIO_ID")

/


CREATE table "VOTANTE" (
    "VOTANTE_ID" NUMBER NOT NULL,
    "NOMBRE"     VARCHAR2(30),
    "CEDULA"     VARCHAR2(20),
    "SEXO"       NUMBER,
    "EDAD"       NUMBER,
    "DIRECCION"  VARCHAR2(30),
    "MESA_ID"    NUMBER NOT NULL,
    constraint  "VOTANTE_PK" primary key ("VOTANTE_ID")
)
/

ALTER TABLE "VOTANTE" ADD CONSTRAINT "VOTANTE_FK" 
FOREIGN KEY ("MESA_ID")
REFERENCES "MESA" ("MESA_ID")

/

CREATE table "JURADO" (
    "JURADO_ID"  NUMBER NOT NULL,
    "NOMBRE"     VARCHAR2(40),
    "CEDULA"     VARCHAR2(20),
    "EDAD"       NUMBER,
    "MESA_ID"    NUMBER,
    "COLEGIO_ID" NUMBER,
    constraint  "JURADO_PK" primary key ("JURADO_ID")
)
/

ALTER TABLE "JURADO" ADD CONSTRAINT "JURADO_FK" 
FOREIGN KEY ("MESA_ID")
REFERENCES "MESA" ("MESA_ID")

/
ALTER TABLE "JURADO" ADD CONSTRAINT "JURADO_FK2" 
FOREIGN KEY ("COLEGIO_ID")
REFERENCES "COLEGIO" ("COLEGIO_ID")

/

